# Optional: for local Jekyll builds (bundle exec jekyll serve)
# GitHub Pages builds the site automatically; this is only if you want to preview locally.

source "https://rubygems.org"
gem "jekyll", "~> 3.9"
gem "just-the-docs"
